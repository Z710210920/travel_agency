<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.dao.UserDao">
	<!-- 目的：为dao接口方法提供sql语句配置 -->
	<!-- namespace是该xml对应的接口全名，select和update中的id对应方法名，
	resultType是返回值类型，parameterType是参数类型（这个其实可选），
	最后#{...}中填写的是方法的参数，
	就是在返回User对象包含了一个属性名为user的User对象，
	那么可以使用"user.属性名"的方式来取值，
	看上面queryByKeyWithBook方法的sql -->
	<select id="queryByName" resultType="User" parameterType="long">
		<!-- 具体的sql -->
		SELECT
			*
		FROM
			user
		WHERE
			name = #{name}
	</select>
	
	<select id="queryAll" resultType="User">
		SELECT
			*
		FROM
			user
		ORDER BY
			id
	</select>
	
	<update id="reduceNumber">
		UPDATE book
		SET number = number - 1
		WHERE
			book_id = #{bookId}
		AND number > 0
	</update>
</mapper>